#include "TeacherDBHeader.h"



GeneralLesson **generateAgendaFromDatabase(char *teacherUsername, int weekIndex) {

    /* 
    RITORNO UN ARRAY NULL TERMINATED di GeneralLesson; evito di costruire altra struct con dentro il numero di elementi
    GeneralLesson **lessonArray = myMalloc(sizeof(GeneralLesson *) * (numRows + 1)) ;
    lessonArray[numRows] = NULL ; 
    */

}